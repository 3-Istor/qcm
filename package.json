{
  "name": "qcm-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "build:nix": "nix-shell --run 'npx prisma generate' && next build",
    "start": "next start",
    "lint": "eslint src",
    "db:generate": "nix-shell --run 'npx prisma generate'",
    "db:push": "nix-shell --run 'npx prisma db push'",
    "db:migrate": "nix-shell --run 'npx prisma migrate dev'",
    "db:studio": "nix-shell --run 'npx prisma studio'",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:build": "docker compose up --build -d",
    "docker:logs": "docker compose logs -f app"
  },
  "dependencies": {
    "@next-auth/prisma-adapter": "^1.0.7",
    "@prisma/client": "6.18.0",
    "clsx": "^2.1.1",
    "lucide-react": "^0.378.0",
    "next": "^16.2.6",
    "next-auth": "^4.24.14",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "tailwind-merge": "^2.3.0",
    "zustand": "^4.5.2"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.3",
    "@types/node": "^26",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^10.9.0",
    "eslint-config-next": "^16.3.2",
    "postcss": "^8",
    "prisma": "7.9.1",
    "tailwindcss": "^4.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5"
  }
}
