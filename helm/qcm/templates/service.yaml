apiVersion: v1
kind: Service
metadata:
  name: { { include "qcm.fullname" . } }
  labels: { { - include "qcm.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "qcm.selectorLabels" . | nindent 4 } }
